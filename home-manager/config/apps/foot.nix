{
    programs = {
        foot = {
            enable = true;
            settings = {
                main = {
                    font = "monospace:size=16";
                    dpi-aware = "yes";
                };

                scrollback = {
                    lines = 10000;
                };

                mouse = {
                    hide-when-typing = "yes";
                };
            };
        };
    };
}
